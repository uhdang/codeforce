#include <iostream>
#include <cmath>

int main() {
  
  int m , n;
  double o;
  std::cin >> m, n;

  o = (m / 2) * n;
  if (m % 2 != 0 && n % 2 != 0) {
    std::floor(o)
  }
  std::cout << o << "\n";


}
